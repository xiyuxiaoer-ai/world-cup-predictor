将「始终用中文回复」的要求写入本项目的 CLAUDE.md，使其在所有后续对话中永久生效。

步骤：
1. 读取项目根目录的 `CLAUDE.md`（如不存在则从空字符串开始）。
2. 检查文件中是否已包含中文回复的相关指令（搜索 "中文" 或 "Chinese"）；若已存在则跳到步骤 4。
3. 在文件**最顶部**插入以下内容（保留原有内容）：

   ```
   # 语言要求

   无论用户使用何种语言提问，你都必须**始终用简体中文**回复。代码、命令、文件路径等技术内容保持原样，只对自然语言说明部分使用中文。

   ```

4. 将修改后的内容写回 `CLAUDE.md`（保留原有格式和缩进）。
5. 用中文向用户确认：已成功配置，Claude 将在本项目中始终使用中文进行沟通。
